BA()=
  -if circle
    BA(double radius )= Math.pi*r*r
  - if rectangle
    BA(double length, double width)= l*w
  - if triangle
    BA(double base, double height)=b*h/2
SA()=
  -if prism
    
  -if pyramdic
    -if cone
      SA(double base,double depth,double sL)=base+pi*r*s
    -non code
      SA(double base,double depth, double SidA)=base+sidA
    

volume()
  -if prism
    - volume(double base, double depth)=b*d
  -if pyramid
    - volume(double b, double d)=b*d

Other()
  totSidA(array sL,double d )= for(int x=0;x<len.sL;x++){
      double holder=0;
      holder=holder+sL[x]*d
    SA=(double base, double depth,sidA)=2b*d+sidA
